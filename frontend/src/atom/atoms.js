import { atom } from 'jotai';

export const authAtom = atom(null);
export const userAtom = atom(null);
export const interviewAtom = atom(null);
export const currentQuestionAtom = atom(null);
export const timerAtom = atom(0);
export const answersAtom = atom([]);
